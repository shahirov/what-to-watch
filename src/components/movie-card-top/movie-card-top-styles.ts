import styled from 'styled-components'

export const StyledMovieCardTop = styled.div`
  position: relative;
  max-width: 1300px;
  margin-top: -170px;
  margin-right: auto;
  margin-left: auto;
  padding: 0 75px;
  z-index: 2;
`

export const MovieCardInfo = styled.div`
  display: -webkit-box;
  display: -ms-flexbox;
  display: flex;
  align-items: flex-start;
`

export const MovieCardDesc = styled.div`
  flex-grow: 1;
`
